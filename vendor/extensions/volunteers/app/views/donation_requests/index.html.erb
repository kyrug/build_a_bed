<h2>New Donation Request</h2>
<% form_for @donation_request do |j| %>
  <%= j.error_messages %>
  <table>
    <tr>
      <td><%= j.label :name %></td>
      <td><%= j.text_field :name %></td>
    </tr>
    <tr>
      <td><%= j.label :description %></td>
      <td><%= j.text_area :description %></td>
    </tr>
    <tr>
      <td><%= j.label :quantity %></td>
      <td><%= j.text_field :quantity %></td>
    </tr>
  </table>
  <%= submit_tag 'Create' %>
<% end %>

<hr />

<h1>All Donation Requests</h1>
<table id="donreqtable">
  <tr>
    <th>Name</th>
    <th>Desc.</th>
    <th>Need</th>
    <th>Avail.</th>
    <th>Admin Actions</th>
  </tr>
<% for donation_request in @donation_requests %>
  <tr class="<%= cycle("odd", "even") %>">
    <td><%= donation_request.name %></td>
    <td><%= donation_request.description %></td>
    <td> <%= donation_request.quantity %></td>
    <td><%= donation_request.quantity_donated %></td>
    <td><%= link_to('edit', :action => 'edit', :id => donation_request.id) %></td>
  </tr>
<% end %>
</table>
