<% form_for @donation_request do |r| %>
  <table>
    <tr>
      <td><%= r.label :name %></td>
      <td><%= r.text_field :name %></td>
    </tr>
    <tr>
      <td><%= r.label :description %></td>
      <td><%= r.text_area :description %></td>
    </tr>
    <tr>
      <td><%= r.label :quantity %></td>
      <td><%= r.text_field :quantity %></td>
    </tr>
  </table>
  <%= submit_tag 'Update' %>
<% end %>

